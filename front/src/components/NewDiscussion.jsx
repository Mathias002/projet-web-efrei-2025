const NewDiscussion = () => {
  const handleNew = () => {
    // Placeholder: open modal or navigate to new discussion page
    console.log('Create new discussion');
  };

  return <button onClick={handleNew}>+ New Discussion</button>;
};

export default NewDiscussion;
